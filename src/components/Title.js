import React from 'react';

const Title = () => {
  return (
  <React.Fragment>
    <h1>My Basic CRUD Site</h1>
    <h3>Here, you can create an entry, view (read) existing entries, update an
    entry, and delete an entry.</h3>
  </React.Fragment>
  );
};

export default Title;